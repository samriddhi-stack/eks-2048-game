# Install EKS
## Using Fargate

<pre><code>eksctl create cluster --name demo-cluster --region us-east-1 --fargate</code></pre>

## Delete the cluster

<pre><code>eksctl delete cluster --name demo-cluster --region us-east-1</code></pre>
